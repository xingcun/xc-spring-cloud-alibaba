spring:
  application:
    name: xc-service
  cloud:
    nacos:
      config:
        server-addr: ${NACOS_ADDR:127.0.0.1}:${NACOS_PORT:8848}
        file-extension: yaml
      
#  profiles:
#    active: dev
    
    
